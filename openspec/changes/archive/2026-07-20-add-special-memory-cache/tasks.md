## 1. 缓存基础结构

- [x] 1.1 在 `utils_cache_memory` 中定义零值可用的 `Cache`、包级 `sync.Map`、8 字节头部常量和负数过期时间错误
- [x] 1.2 实现固定字节序的条目编码与解码辅助函数，保证写入仅复制一次业务数据且读取返回数据子切片
- [x] 1.3 封装可替换的当前时间来源和过期判定逻辑，支持正数绝对时间及 `0` 永不过期语义

## 2. 并发缓存 API

- [x] 2.1 实现 `Set` 和 `Get`，覆盖输入切片所有权、过期未命中及读取时不自动删除
- [x] 2.2 实现 `Delete` 和 `LoadAndDelete`，确保取出操作只返回有效条目
- [x] 2.3 实现 `Range`，仅回调有效条目并支持回调提前停止
- [x] 2.4 实现 `DeleteExpired`，按指定 Unix 秒以弱一致性方式删除过期条目并返回删除数量
- [x] 2.5 为所有导出标识符补充 GoDoc，明确全局共享、key 可比较性及返回切片只读契约

## 3. 自动化验证

- [x] 3.1 添加单元测试，覆盖零值使用、跨实例共享、字节布局、空数据、输入切片隔离和非法过期时间
- [x] 3.2 添加可控时间下的单元测试，覆盖有效、永不过期、过期边界、读取不删除和主动批量清理
- [x] 3.3 添加删除、原子取出、遍历提前停止及过期过滤测试
- [x] 3.4 添加并发存取测试，并使用 `go test -race` 检查数据竞争
- [x] 3.5 添加 `Set`、命中 `Get` 和过期 `Get` benchmark，并报告内存分配

## 4. 文档与最终检查

- [x] 4.1 更新 `utils_cache_memory/README.md`，记录 API 示例、时间语义、全局共享限制、按需清理方式和只读切片约束
- [x] 4.2 运行 `gofmt`、包级测试、竞态测试及 benchmark，确认 Go 1.19 兼容且无新增第三方依赖
